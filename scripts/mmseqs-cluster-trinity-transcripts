#!/bin/bash

docker run --rm \
  --mount type=bind,source="$PWD",target=/work \
  ghcr.io/soedinglab/mmseqs2 \
  easy-cluster /work/$1 /work/$1 /tmp \
  --min-seq-id 0.95 -c 0.85 --cov-mode 1
rm $1_all_seqs.fasta
mv $1_rep_seq.fasta $1_rep_seq.fna
gzip $1_rep_seq.fna
